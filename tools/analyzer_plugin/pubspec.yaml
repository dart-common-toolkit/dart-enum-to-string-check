name: dart_enum_to_string_check_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 2.1.0

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  dart_enum_to_string_check:
    path:
