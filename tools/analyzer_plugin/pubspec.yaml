name: dart_enum_to_string_check_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 0.8.0
homepage: https://github.com/fartem/dart-enum-to-string-check

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  dart_enum_to_string_check:
    git:
      url: https://github.com/fartem/dart-enum-to-string-check
      ref: 0.8.0
